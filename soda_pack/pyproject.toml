[tool.poetry]
name = "soda-pack"
version = "1.0.0"
description = "Check validity format for soda"
authors = ["QALITA SAS <contact@qalita.io>"]
license = "Apache-2.0"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
soda-core-pandas-dask = "^3.1.5"
qalita-core = "^0.3.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
